# Stella Glow 한국어화 v31 LayeredFS 빌드·검증 보고서

> **이 문서는 v31 시점의 기록입니다.** v31.1에서 `code.ips`를 배포에서 제외했으므로
> 아래의 "추가 파일 `code.ips` 1개" 항목은 현재 배포본에 해당하지 않습니다.
> 사유는 CHANGELOG의 v31.1 항목을 보십시오.

- 작성일: 2026-08-04
- 대상 타이틀 ID: `0004000000173700`
- 기준 릴리스: v30
- 배포 형식: Luma3DS/Azahar용 LayeredFS 패치

## 1. 결과

| 항목 | 결과 |
|---|---|
| RomFS 패치 파일 | 1,470개 |
| 추가 파일 | `code.ips` 1개 |
| 패치 데이터 크기 | 21,196,253 bytes |
| 원본 전체 RomFS 파일 | 26,997개 |
| 파일별 크기·SHA-256 검증 | PASS |
| 작업 스테이징과 Azahar 설치본 비교 | 1,471/1,471 일치 |

v31은 완성 CIA나 게임 전체를 포함하지 않는다. 사용자가 보유한 영어판 Undub
`CTR-P-BS3E`에 적용하는 LayeredFS 차분이다.

## 2. 대사 복원 및 문맥 검수

- 폰트 개선 이전의 폭 제한 때문에 축약됐던 대사 919건을 복원했다.
- 이벤트 31,821행을 일본어 원문·영문판·한국어판으로 재대조했다.
- 변경 이벤트의 제어 구조와 문자열 슬롯을 보존했다.
- 한국어 대사 폰트 기준 전수 레이아웃 검사에서 대사창 초과는 0건이었다.
- 확정 용어, 조사, 문맥 및 초기 서술 교정을 후속 병합했다.

## 3. 폰트 및 UI

- 대사 폰트의 한글 본문 획 알파를 영문 본문과 같은 수준으로 보정했다.
- 자동 보정으로 건드리기 위험한 `같`, `떻`, `몇`의 흐린 획은 지정 픽셀만 보강했다.
- 글리프 매핑·문자 폭·파일 크기와 텍스처 밖 데이터가 유지됨을 확인했다.
- `설정` 버튼과 `소지금` 라벨은 원본 라운드·경사 형상을 보존하고 잉크만 교체했다.
- HP/SP, 문장부호, 괄호 및 영문 글자의 문자 폭 회귀를 정규화했다.
- 긴 아이템·오브 설명이 겹치지 않도록 도움말 표시 영역을 확장했다.
- 불·물·바람·땅 강화와 HP 재생 라벨은 원본 윤곽·그림자를 유지하며 색상만 구분했다.

## 4. 명칭·설명·수치

- 아이템, 스킬, 오브 설명을 런타임 파라미터와 대조했다.
- 영어 일반명사로 남은 적 이름 24종을 한국어화했다.
- 사용자 확정 스킬 위력과 상태이상 확률 조정을 반영했다.
- 사각·자객·은자·필중 오브를 포함한 상태·버프 설명에 실제 판정과 효과를 명시했다.
- 문자열 풀의 순서 인덱스 호환성과 확장 인덱스 범위를 검증했다.

## 5. 정적 검증

1. 전체 26,997개 원본 RomFS와 최종 차분을 비교했다.
2. 변경 RomFS 1,470개의 원본/패치 크기와 SHA-256을 기록했다.
3. `code.ips`의 크기와 SHA-256을 별도 기록했다.
4. 최종 작업 스테이징과 Azahar 설치본의 파일 수 및 해시가 일치함을 확인했다.
5. `verify_patch.ps1`로 배포 트리의 1,471개 파일을 재검증한다.

## 6. 남은 런타임 확인

- 장시간 플레이에서 복원 대사의 문맥과 줄바꿈
- 흐린 획을 보강한 글자의 실기 가독성
- `설정`, `소지금` 라벨의 경계와 중앙 정렬
- 조정된 스킬·상태이상·오브 수치의 실제 전투 반영
